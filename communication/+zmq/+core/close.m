% zmq.core.close- Close a ZMQ socket
%
% Usage: status = zmq.core.close(socket)
%
% Input: socket - Instantiated ZMQ socket handle to be closed (see zmq.core.socket).
%
% Output: Zero if successful, otherwise -1.
%
